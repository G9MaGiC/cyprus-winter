/**
 * Central list of "best for" filter options (e.g. for discover filters).
 * Data files use free-form bestFor strings; this is for UI filter chips.
 * Empty by default — add when a filter chip UI is built.
 */
export const BEST_FOR_OPTIONS: { slug: string; label: string }[] = [];
